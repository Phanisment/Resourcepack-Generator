name: Texture Mod
description: generated pack by ItemCaster.
format: 15

generate:
  enable: false
  atlas_type: directory # directory, single
  hosting:
    self-host: false
    lobfile: true
  zip:
    protect: false

download_state:
  prompt: |
  force_use: false
  player_protection:
    invincible: false # Its has risk eazly to abuse.

start_model_data: 1000
merge:
- ModelEngine/resource pack
#- BetterHUD/

addtitional:
  negative_space: true